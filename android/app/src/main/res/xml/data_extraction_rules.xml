<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ split backup into cloud backup and device-to-device transfer.
  Both are refused for the same reason as backup_rules.xml: the offline
  cache holds the user's financial history in the clear, and a self-hosted
  server is the only place it is meant to live.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
    </device-transfer>
</data-extraction-rules>
